[🇺🇸 English Version](./README.md)

<div dir="rtl" align="right">

# سوال: H. حذف درخت جام مقدس
 [**🔗 لینک سوال**](https://codeforces.com/problemset/problem/2193/H)

### ⚙️ جزئیات
| ویژگی | مقدار |
| :--- | :--- |
| **زمان** | 3 seconds |
| **حافظه** | 256 megabytes |
| **تگ‌ها** | `dfs and similar`, `dp`, `graphs`, `greedy`, `implementation`, `trees`, `*2400` |

---
### 📖 صورت سوال
درخت بزرگ گریل به مدت $315$ سال در پادشاهی ایستاده است. این درخت فضای زیادی اشغال کرده است، بنابراین پادشاه ایلا تصمیم گرفت هر چه سریع‌تر از شر آن خلاص شود. خود درخت یک گراف غیرجهت‌دار، همبند و بدون دور با $n$ رأس است که هر رأس آن دارای مقدار $a_v$ مختص به خود می‌باشد. درخت به روش زیر قابل حذف است:

- $S_v$ را مجموع مقادیر تمامی همسایه‌های باقیمانده‌ی `v` در نظر بگیرید. اگر `v` هیچ همسایه‌ی باقیمانده‌ای نداشته باشد، آنگاه $S_v$ برابر با $0$ است. رأسی مانند `v` را انتخاب کنید که $a_v$ و $S_v$ در زوجیت (parity) متفاوت باشند (یعنی یا $a_v$ زوج و $S_v$ فرد باشد، یا $a_v$ فرد و $S_v$ زوج باشد). اگر چنین رئوسی وجود نداشت، فرآیند را متوقف کنید.
- رأس `v` و تمامی یال‌های متصل به آن را از درخت حذف کنید.

وظیفه شما این است که مشخص کنید آیا دنباله‌ای از حذف‌ها وجود دارد که منجر به حذف کامل درخت گریل شود (یعنی هیچ رأسی در درخت باقی نماند). اگر چنین دنباله‌ای وجود دارد، دنباله‌ی $n$ رأس را به ترتیب حذفشان خروجی دهید. اگر چندین پاسخ وجود دارد، هر یک از آن‌ها را خروجی دهید.

### 📥 مشخصات ورودی
هر تست از چندین مورد آزمایشی تشکیل شده است. اولین خط شامل یک عدد صحیح $t$ ($1\le t\le 10^4$) است — تعداد موارد آزمایشی. توصیف موارد آزمایشی به شرح زیر است.

اولین خط شامل عدد $n$ ($1\le n\le 2\cdot 10^5$) است — تعداد رئوس در درخت گریل (Grail Tree).

خط دوم آرایه $a$ ($1\le a_i\le 10^9$) را توصیف می‌کند — مقادیر رئوس در درخت.

در ادامه، $n - 1$ خط وجود دارد که هر کدام شامل 2 عدد `v` و `u` ($1\le v, u\le n, v\neq u$) هستند و نشان می‌دهند که رئوس `v` و `u` توسط یک یال در درخت به هم متصل شده‌اند.

تضمین می‌شود که مجموع `n` در تمامی موارد آزمایشی از $2\cdot 10^5$ تجاوز نمی‌کند.

### 📤 مشخصات خروجی
برای هر تست‌کیس، اگر امکان‌پذیر است که درخت گریل (Grail Tree) را به طور کامل حذف کنید، "YES" را خروجی دهید. در غیر این صورت، "NO" را خروجی دهید. اگر پاسخ "YES" است، هر توالی از حذف‌ها را خروجی دهید.

می‌توانید هر حرف را با هر ترکیبی از حروف کوچک یا بزرگ (lowercase or uppercase) خروجی دهید. برای مثال، رشته‌های "yEs", "yes", "Yes", و "YES" به عنوان پاسخ مثبت پذیرفته خواهند شد.

</div>

---
### 🧪 نمونه‌ها
#### نمونه 1
| ورودی (Input) | خروجی (Output) |
| :--- | :--- |
| <pre>5<br>3<br>1 2 4<br>1 2<br>2 3<br>4<br>3 4 2 1<br>1 2<br>2 3<br>3 4<br>6<br>9 6 5 1 7 4<br>1 2<br>2 3<br>2 4<br>3 5<br>4 6<br>5<br>2 1 1 1 2<br>2 1<br>3 2<br>2 4<br>5 4<br>5<br>1 5 3 7 9<br>1 2<br>2 3<br>3 4<br>4 5</pre> | <pre>NO<br>YES<br>2 3 1 4<br>NO<br>YES<br>1 5 2 3 4<br>YES<br>2 4 1 5 3</pre> |

<div dir="rtl" align="right">

---
*توجه: این صورت سوال‌ توسط ربات دریافت و ترجمه شده است.*

</div>
